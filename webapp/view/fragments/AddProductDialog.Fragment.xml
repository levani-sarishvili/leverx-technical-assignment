<core:FragmentDefinition
  xmlns="sap.m"
  xmlns:f="sap.ui.layout.form"
  xmlns:core="sap.ui.core">
  <Dialog
    id="createProductDialog"
    title="{i18n>productDialogTitle}"
    resizable="true"
    contentWidth="28rem"
  >
    <f:SimpleForm
      id="productDialogForm"
      layout="ResponsiveGridLayout"
      editable="true"
      labelSpanL="4"
      labelSpanM="12"
      columnsL="2"
      columnsM="1"
    >
      <f:content>
        <!-- Name -->
        <Label text="{i18n>productNameLabel}" labelFor="name" />
        <Input
          value="{
            path: 'appStateModel>Name',
            type: 'sap.ui.model.type.String',
            constraints: {
              minLength: 3,
              maxLength: 100,
              required: true
             } 
            }"
          placeholder="{i18n>productNameInputPlaceholder}"
          type="Text"
          fieldGroupIds="productFormGroup"
          required="true" />

        <!-- Price -->
        <Label text="{i18n>productPriceLabel}" labelFor="price" />
        <Input
          value="{
            path: 'appStateModel>Price',
            type: 'sap.ui.model.type.Integer',
            constraints: {
              minimum: 1,
              maximum: 100000,
              required: true
              }
            }"
          placeholder="{i18n>productPriceInputPlaceholder}"
          fieldGroupIds="productFormGroup"
          type="Number"
          required="true" />

        <!-- Category -->
        <Label text="{i18n>productCategoryLabel}" labelFor="category" />
        <Select
          width="100%"
          forceSelection="false"
          required="true"
          fieldGroupIds="productFormGroup"
          selectedKey="{
            path: 'appStateModel>Category',
            type: 'sap.ui.model.type.String',
            constraints: {
              required: true,
              minLength: 2
             } 
            }"
          items="{
							path: '/ProductCategories',
							sorter: { path: 'Name' }
						}">
          <core:Item key="{Key}" text="{Name}" />
        </Select>

        <!-- StockStatus -->
        <Label text="{i18n>productStockStatusLabel}" labelFor="stockStatus" />
        <Select
          width="100%"
          forceSelection="false"
          required="true"
          fieldGroupIds="productFormGroup"
          selectedKey="{
            path: 'appStateModel>StockStatus',
            type: 'sap.ui.model.type.String'          
            }"
          items="{
							path: '/ProductStockStatuses',
							sorter: { path: 'Name' }
						}">
          <core:Item key="{Key}" text="{Name}" />
        </Select>

        <!-- Brand -->
        <Label text="{i18n>productBrandLabel}" labelFor="brand" />
        <Select
          width="100%"
          forceSelection="false"
          required="true"
          fieldGroupIds="productFormGroup"
          selectedKey="{
            path: 'appStateModel>Brand',
            type: 'sap.ui.model.type.String',
            constraints: {
              required: true,
              minLength: 2
             } 
            }"
          items="{
							path: '/ProductBrands',
							sorter: { path: 'Name' }
						}">
          <core:Item key="{Key}" text="{Name}" />
        </Select>

        <!-- SupplierName -->
        <Label text="{i18n>productSupplierLabel}" labelFor="supplierName" />
        <Input
          fieldGroupIds="productFormGroup"
          value="{
            path: 'appStateModel>SupplierName',
            type: 'sap.ui.model.type.String',
            constraints: {
              minLength: 3,
              maxLength: 100,
              required: true
             } 
            }"
          placeholder="{i18n>productSupplierInputPlaceholder}"
          type="Text"
          required="true" />

        <!-- ReleaseDate -->
        <Label text="{i18n>productReleaseDateLabel}" labelFor="DP1" />
        <DatePicker
          id="DP1"
          placeholder="{i18n>productReleaseDateInputPlaceholder}"
          displayFormat="dd/MM/yyyy"
          valueFormat="yyyy-MM-dd"
          value="{
            path: 'appStateModel>ReleaseDate',
            type: 'sap.ui.model.type.Date',
            formatOptions: {
              pattern: 'yyyy-MM-dd'
            }
          }"
          fieldGroupIds="productFormGroup"
          required="true" />

        <!-- Rating -->
        <Label text="{i18n>productRatingLabel}" labelFor="rating" />
        <Input
          value="{
            path: 'appStateModel>Rating', 
            type: 'sap.ui.model.type.Float', 
            constraints: { 
              minimum: 1, 
              maximum: 5,
              required: true 
              }
            }"
          placeholder="{i18n>productRatingInputPlaceholder}"
          type="Number"
          fieldGroupIds="productFormGroup"
          required="true" />
      </f:content>
    </f:SimpleForm>

    <buttons>
      <Button
        type="Accept"
        text="{i18n>createButtonText}"
        press="onProductCreatePress" />
      <Button
        type="Reject"
        text="{i18n>cancelButtonText}"
        press="onProductCancelPress" />
    </buttons>
  </Dialog>
</core:FragmentDefinition>